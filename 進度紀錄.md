# 進度紀錄

## 2025-12-25
- 訂閱清單新增付款狀態切換（已繳 / 已過），並保留本期狀態；跨期自動重置。
- 匯入功能：新增 Import 按鈕與視窗，支援 CSV/JSON 解析與預覽（新增/更新/略過）。
- CSV 去重規則：訂閱以「平台名稱」為 key；付款以「平台 + 日期」為 key；內容不同則更新。
- 通知設定新增寄送佇列操作（已寄送 / 重新排程）與通知紀錄卡，紀錄存入 localStorage（key: subpulse.notifications）。
- 付款記錄區塊刪除按鈕跑版修正。

